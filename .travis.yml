language: java

sudo: false

jdk: oraclejdk8

script: 
- mvn clean
- mvn install
- mvn jar:jar
- mvn cobertura:cobertura

branches:
  only:
  - feature-maven-subprojects

after_success:
- bash <(curl -s https://codecov.io/bash)
