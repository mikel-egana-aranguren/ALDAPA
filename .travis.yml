language: java

sudo: false

jdk: oraclejdk8

script: 
- mvn clean
- mvn install
- mvn cobertura:cobertura

branches:
  only:
  - feature-get-multiple-project-catalog-dataset-named-graph

after_success:
- bash <(curl -s https://codecov.io/bash)
